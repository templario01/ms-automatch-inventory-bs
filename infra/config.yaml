apiVersion: v1
kind: Namespace
metadata:
  name: automatch

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: automatch-inventory-bs-configmap
  namespace: automatch
data:
  NODE_ENV: "staging"
  PORT: "3030"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: automatch-inventory-bs-deploy
  namespace: automatch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-automatch-inventory-bs
  template:
    metadata:
      labels:
        app: ms-automatch-inventory-bs
    spec:
      containers:
        - name: ms-automatch-inventory-bs
          image: 020503494232.dkr.ecr.us-east-1.amazonaws.com/ms-automatch-inventory-bs
          imagePullPolicy: Always
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: automatch-inventory-bs-configmap
                  key: NODE_ENV
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: automatch-inventory-bs-configmap
                  key: PORT
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: automatch-inventory-bs-secrets
                  key: DATABASE_URL
          ports:
            - containerPort: 3030
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi" # max RAM = 512 Mb
              cpu: "300m" # max CPU-Cores = 0.3 GB

---
apiVersion: v1
kind: Service
metadata:
  name: automatch-inventory-bs-svc
  namespace: automatch
spec:
  selector:
    app: ms-automatch-inventory-bs
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3030
  type: ClusterIP #[ClusterIP]Expose node for internal k8 comunication, [LoadBalancer]Expose node for external comunication

