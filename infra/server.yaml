apiVersion: v1
kind: ConfigMap
metadata:
  name: automatch-inventory-bs-configmap
  namespace: automatch
data:
  NODE_ENV: "production"
  PORT: "3030"
  DATABASE_URL: "mongodb+srv://victbenavente:023FjoIZxpOQFKEa@automatch-mongo-cluster.tjppmyq.mongodb.net/automatch-inventory?retryWrites=true&w=majority"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: automatch-inventory-bs-deploy
  namespace: automatch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-automatch-inventory-bs
  template:
    metadata:
      labels:
        app: ms-automatch-inventory-bs
    spec:
      automountServiceAccountToken: false
      containers:
        - name: ms-automatch-inventory-bs
          image: victor0141/ms-automatch-inventory-bs:1.2
          imagePullPolicy: Always
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: automatch-inventory-bs-configmap
                  key: NODE_ENV
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: automatch-inventory-bs-configmap
                  key: PORT
            - name: DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: automatch-inventory-bs-configmap
                  key: DATABASE_URL
          ports:
            - containerPort: 3030
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m" 

---
apiVersion: v1
kind: Service
metadata:
  name: automatch-inventory-bs-svc
  namespace: automatch
spec:
  selector:
    app: ms-automatch-inventory-bs
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3030
  type: ClusterIP

